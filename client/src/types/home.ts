export type HomeMetric = {
  value: string;
  label: string;
};

export type HomeProject = {
  title: string;
  location: string;
  image: string;
};

export type HomeService = {
  title: string;
  description: string;
};
