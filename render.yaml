services:
  - type: web
    name: photography-platform-backend
    env: node
    rootDirectory: server
    plan: free
    region: oregon
    buildCommand: npm install && npm run build
    preDeployCommand: npm run seed:admin
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
